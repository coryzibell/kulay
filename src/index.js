var MersenneTwister = require('mersennetwister');
var mt = new MersenneTwister();

import colors from 'accessibleNamedColors'

export default class Kulay {
  constructor() {
    this.pair = colors
    console.log(this.pair)
  }
}